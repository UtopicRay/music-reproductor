---
interface Props {
  color: string;
  genre: string;
}
const { color, genre } = Astro.props;
---

<div class={`flex justify-center items-center w-32 h-20 bg-[${color}] text-center text-white rounded-lg font-bold text-lg`}>
  <h1>{genre}</h1>
</div>
